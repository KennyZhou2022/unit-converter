{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/KennyZhou2022/unit-converter/main/schemas/conversions.schema.json",
  "title": "nist-unit-converter package data",
  "type": "object",
  "required": ["version", "source", "conversions"],
  "additionalProperties": false,
  "properties": {
    "version": {
      "type": "integer",
      "const": 2
    },
    "source": {
      "type": "object",
      "required": ["name", "document"],
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": ["string", "null"]
        },
        "document": {
          "type": ["string", "null"]
        }
      }
    },
    "conversions": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["from", "to"],
        "additionalProperties": false,
        "properties": {
          "from": {
            "type": "string",
            "minLength": 1
          },
          "to": {
            "type": "string",
            "minLength": 1
          },
          "factor": {
            "type": "string",
            "pattern": "^[+-]?(?:\\d+(?:\\.\\d*)?|\\.\\d+)(?:[eE][+-]?\\d+)?$"
          },
          "formula": {
            "type": "string",
            "minLength": 1
          }
        },
        "oneOf": [
          {
            "required": ["factor"],
            "not": {
              "required": ["formula"]
            }
          },
          {
            "required": ["formula"],
            "not": {
              "required": ["factor"]
            }
          }
        ]
      }
    }
  }
}
